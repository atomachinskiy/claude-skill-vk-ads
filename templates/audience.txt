# Список контактов для remarketing users_list.
# Один контакт на строку. Пустые строки и '#' игнорируются.
# Телефоны — любой формат (+7, 8, пробелы, скобки), скрипт нормализует в 7XXXXXXXXXX.
# Emails — будут приведены к lower-case.
# Перед отправкой SHA256-хешируются; сырые данные в VK не попадают.

+7 (999) 123-45-67
8-999-123-45-68
user@example.com
